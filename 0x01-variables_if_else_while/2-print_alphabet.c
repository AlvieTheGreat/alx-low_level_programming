#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints alphabet in lowercase
 *
 * Return: Always zero (Success)
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
	putchar('a');
	putchar('\n');
	return (0);
}
